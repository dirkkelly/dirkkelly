comments:
  allowedFields: ["name", "email", "body"]
  allowedOrigins: ["localhost", "dirkkelly.com"]
  branch: "master"
  generatedFields:
    date:
      type: date
  format: "json"
  commitMessage: "Staticman Comment"
  moderation: true
  name: "Dirk Kelly"
  path: "data/comments/{options.entryId}"
  filename: "{@id}"
  requiredFields: ["name", "body"]
  transforms:
    email: md5
